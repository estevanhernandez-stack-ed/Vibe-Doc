---
description: Show current Vibe Doc status
---

Run `npx @esthernandez/vibe-doc-cli status` in the user's project directory via bash. Present the output in a clean format showing last scan time, classification, and documentation coverage.

If no state exists, tell the user to run `/scan` first.
